# ── Storefront env (Vite — only VITE_* are exposed to the browser) ──
# Copy to .env.local for dev; set the same vars in the host for prod.

# Supabase (PUBLIC anon key — safe in browser; tables protected by RLS).
VITE_SUPABASE_URL=https://bigmdvjrvqyqzyrijdum.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
